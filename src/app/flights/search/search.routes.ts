import { Routes } from '@angular/router';
import { SearchFlightsComponent } from './search.component';

export const searchRoutes: Routes = [
  { path: '', component: SearchFlightsComponent }
];
